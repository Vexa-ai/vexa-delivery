# SPDX-License-Identifier: Apache-2.0
# gcp-gke — DECLARED profile: named deltas only, not yet exercised end-to-end.
PROFILE_TESTED=no
ARGOCD_VERSION=v3.5.1
KYVERNO_VERSION=v1.19.0
# known deltas to verify on first real install:
# - Autopilot clusters enforce their own mutation/limits — preflight P2/P4
#   findings differ from Standard; start with Standard
# - default StorageClass (standard-rwo) present by default
