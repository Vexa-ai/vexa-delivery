# SPDX-License-Identifier: Apache-2.0
# aws-eks — DECLARED profile: named deltas only, not yet exercised end-to-end.
PROFILE_TESTED=no
ARGOCD_VERSION=v3.5.1
KYVERNO_VERSION=v1.19.0
# known deltas to verify on first real install:
# - EBS CSI addon must be installed for a default StorageClass (gp3)
# - LoadBalancer needs the AWS Load Balancer Controller (or classic ELB)
# - ECR pull-through cache is the natural image mirror for the channel
