# SPDX-License-Identifier: Apache-2.0
# azure-aks — DECLARED profile: named deltas only, not yet exercised end-to-end.
PROFILE_TESTED=no
ARGOCD_VERSION=v3.5.1
KYVERNO_VERSION=v1.19.0
# known deltas to verify on first real install:
# - default StorageClass (managed-csi) present by default
# - Azure Policy add-on may enforce its own admission on top of Kyverno
